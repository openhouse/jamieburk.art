import { Hero } from '@/components/Hero'; import { ProofStrip } from '@/components/ProofStrip'; import { CapabilityGrid } from '@/components/CapabilityGrid'; import { WorkCard } from '@/components/WorkCard'; import { ContactCTA } from '@/components/ContactCTA'; import { getAllWork } from '@/lib/content';
export default function Home(){const works=getAllWork().slice(0,6); return <><Hero/><ProofStrip/><CapabilityGrid/><section className="my-16"><h2 className="text-3xl font-bold">Selected systems</h2><div className="mt-6 grid gap-6 md:grid-cols-2">{works.map(w=><WorkCard work={w} key={w.slug}/>)}</div></section><ContactCTA/></>}
