import { PublicSafetyNote } from './PublicSafetyNote'; import { TagList } from './TagList'; import type { WorkFrontmatter } from '@/lib/types';
export function CaseStudyLayout({work, children}:{work:WorkFrontmatter; children:React.ReactNode}){return <article className="mx-auto max-w-3xl"><p className="font-semibold text-broadway">{work.role} · {work.years}</p><h1 className="mt-3 text-5xl font-bold">{work.title}</h1><p className="mt-5 text-xl text-muted">{work.summary}</p><div className="mt-5"><TagList tags={work.tags}/></div><PublicSafetyNote/><div className="prose prose-lg max-w-none">{children}</div></article>}
