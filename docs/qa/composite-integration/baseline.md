# Composite Integration Baseline

**Date:** July 16, 2026

**Candidate:** `68846b1f0a8672ff80598a7fef50ce5343c3e004`

Before composite work, the untouched `feature/knowledge-l` candidate passed
`npm run check` on Node 26.5.0. Existing citation, knowledge-lifecycle,
public-safety, route, corpus, Chad-lens, blind-spot, Margaret Morse, Warren Sack,
and recursive checks all passed.

The new composite contract was intentionally introduced before its governance
repairs. Its first run produced **68/100** with three hard-gate failures. The
test suite passed all seven anti-gaming and fail-closed tests. The scorecard
identified these concrete gaps:

- explicit lifetime-archive and impact boundaries for recovered corpora;
- a collective-credit policy covering every projected project;
- an exact-route projection-surface manifest;
- separate application-share and production decisions;
- composite npm commands wired into the root check;
- routine public-safe intake and lifecycle-query commands;
- candidate-bound composite scorecards after the repairs.

Those failures are the starting objective, not regressions in the pre-existing
portfolio.
