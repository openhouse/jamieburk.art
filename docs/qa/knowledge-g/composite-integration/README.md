# `feature/knowledge-g` composite integration record

## Candidate

- Branch: `feature/knowledge-g`
- Starting point: `feature/evals-G` at `d97055d72d794b9ecfd338e10277e4feb14ea918`
- Candidate fingerprint: `5f8b0109d100fd48b70168e602061b241f2371bb4941f757cf6aa15d6f624772`
- Composite rubric fingerprint: `5dd84a3b8a91c8c1e2cccd2e281c7e59a3c2494ebaf6fe06ca250fdd2be09b11`
- Knowledge candidate fingerprint: `e3f163b7145084b1bfda21a7a6af62e684e3687de07a05f91101f6071457a535`

The frozen `feature/evals-A` through `feature/evals-N` refs were inventoried and
read without merging or modifying them. Their durable strengths were integrated
through one composite suite, not through parallel canonical registries.

## Recursive hill climb

The loop repaired, in order:

1. structural integration and exact-candidate evidence;
2. WOW List adoption language and first-hand KC Town Hall attribution;
3. archive user-row wording and actor/result distinctions;
4. Chad-lens hiring hierarchy through a flagship commercial proof;
5. Margaret Morse and Warren Sack continuity through a concrete embodied-practice example;
6. the final TypeScript narrowing defect in the flagship proof resolver.

The exact-candidate independent holdout awarded `4/4` to `CI-007`, `CI-008`,
and `CI-009`. Two unchanged final composite runs each scored `0.98` against a
`0.90` threshold. The second run records `consecutive_local_pass: true`.

The knowledge-development run scored `0.9325` against a `0.85` threshold.

## Verification

- `npm run check`: pass
- `npm run preflight:staging`: pass
- `npm run preflight:production`: pass
- Staging Docker image: `jamieburk-art:knowledge-g@sha256:5a56ceaa1949e0e719537ba01c687e476bfc34d62f0ccec2fd6eb3b31616e6ee`
- Runtime endpoints: 17 intended endpoints returned `200`; `/proofs` returned the expected `404`
- Responsive audit: 14 routes across 4 viewports, 56 passing route/width pairs
- Responsive assertions: one `h1`, one `main`, no horizontal overflow, no duplicate IDs, no broken fragments, and no broken rendered images
- Visual inspection: mobile and desktop homepage hierarchy plus desktop About continuity passed

`npm audit --omit=dev` reports two moderate PostCSS advisories nested under
Next.js. npm proposes a forced breaking downgrade to Next 9, so no automated
dependency mutation was accepted in this candidate.

## Held authority

The recursive loop stopped because the remaining gates require people, not more
optimizer passes:

- `CI-012`: exact-candidate human approval and production release authority;
- `KD-015`: observed hiring-reader comprehension;
- `KD-018`: a rights-cleared visual evidence edit.

The candidate passes every local gate. It does not claim those human decisions
have occurred, and it does not create a public `/proofs` route.

## Knowledge Wiki source-return extension

The July 19 Knowledge Wiki source-return and missing-pages candidate is bound
to composite fingerprint
`d347d31202a0e8ac6fde0dfbd0084680e997c97531214903eb6833aa5e402cef`.
It adds project and campaign narratives, cross-project practice and governance
pages, and present-tense original-source rereading without changing the public
website projection.

- staging and production environment contracts: pass;
- exact-candidate Docker image:
  `jamieburk-art:knowledge-wiki-C-missing-pages@sha256:392b5289adef119c530a9b1a540bc144ee269d471f634844412b2e7c8dc023b7`;
- 18 intended runtime endpoints: `200`;
- prohibited `/proofs` route: expected `404`;
- 56 responsive route-width pairs: pass with zero route, image, fragment,
  landmark, overflow, duplicate-ID, or browser-console failures;
- two exact-candidate composite passes: `0.98`, above the `0.90` threshold;
  the second records `consecutive_local_pass: true`;
- fresh exact-candidate independent scores: `4/4` for `CI-007`, `CI-008`,
  and `CI-009`;
- final bounded composite score: `0.98`.

`CI-012` remains open for Jamie's exact-candidate authority. The extension is
engineering-ready and human-held; it is not a publication or production
release decision.
