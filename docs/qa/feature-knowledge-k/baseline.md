# `feature/knowledge-k` composite baseline

**Captured:** 2026-07-16  
**Revision:** `5b0cbcd1a7f913d2281c671e46903ad997a743f5`  
**Node:** `v26.5.0`  
**npm:** `11.3.0`

## Untouched result

The branch matched both `origin/feature/knowledge-k` and
`origin/feature/evals-K` at the pinned revision. The worktree was clean before
dependency installation.

Fresh `npm ci` completed. Fresh `npm run check` passed, including:

- 14 citation tests;
- 6 eval-runner tests;
- 53 knowledge-lifecycle tests;
- all social and full-population corpus evaluators at their declared criteria;
- blind-spot governance at `100/100` with no false closures and closure progress
  reported separately at `10/100`;
- visual-evidence freshness across 42 route/viewport observations and 24
  source-bound captures;
- TypeScript, ESLint, Next.js production build, compiled-lifecycle leak checks,
  knowledge-bank validation, public safety, and route checks.

The fast portfolio, knowledge-lifecycle, Chad, Margaret Morse, and Warren Sack
evaluators passed their deterministic gates. None reached its semantic stop
condition because no fresh exact-revision scorecard pair was supplied.

## Measured composite gap

The baseline did not contain:

1. a family-level A-N integration contract;
2. final behavior-level donor dispositions;
3. an adversarial mutation suite for composite failure modes;
4. a CI gate joining the composite and existing focused checks; or
5. fresh exact-candidate semantic judgments joining reader value, correction
   resilience, archive integrity, and honest human status.

The composite rubric was frozen before implementation so these gaps can be
measured rather than narrated away.

