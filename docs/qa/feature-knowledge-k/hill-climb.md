# Feature knowledge K hill-climb record

**Pinned start:** `5b0cbcd1a7f913d2281c671e46903ad997a743f5`
**Branch:** `feature/knowledge-k`
**Evaluation date:** 2026-07-16
**Production authority:** not granted

## Iteration 0 - untouched K baseline

The clean K candidate passed the existing root check, 14 citation tests, 6 eval
runner tests, 53 lifecycle tests, every deterministic social-corpus evaluator,
the 42-observation visual-evidence check, and the Next.js production build.

The baseline did not satisfy a composite stop condition. It had no A-N behavior
ledger, no locked composite rubric, no adversarial integration contract, no
independent composite scorecard pair, and no candidate-specific CI workflow.
Human and external blind-spot evidence also remained explicitly open.

## Iteration 1 - composite governance

The first repair added:

- a frozen 14-criterion, 100-point composite rubric;
- exact-SHA dispositions for `feature/evals-A` through `feature/evals-N`;
- a machine-readable human-status boundary with six non-agent gates;
- recent-capability, outcome-transfer, artistic-continuity, and release-status
  planning maps;
- a Node 26 pull-request workflow with clean install, root check, PDF tooling,
  and staging and production-mode preflights; and
- 13 mutation tests covering missing donors, weak filename mappings, stale
  revisions, false human closure, rubric drift, incomplete CI, evidence upload,
  and missing planning boundaries.

Result: composite governance reached 100/100, all 14 donors received final
dispositions, zero human or external gates were falsely closed, and the full
repository check passed. The stop condition remained open for rendered and
independent semantic evidence.

## Iteration 2 - rendered defect and repair

An extended Playwright pass inspected 14 public routes at 1440 by 900 and 390
by 844. It found four desktop case studies whose narrow two-column At a Glance
values exceeded their 120-pixel columns. The document did not widen, but local
text borrowed space from the neighboring column.

The accepted repair added a real minimum-width boundary and word wrapping to
the shared At a Glance value cells. It preserved the compact layout and the
truthful role language.

The same 28-observation pass then returned zero failures: every route returned
200, carried a title and one H1, retained one main landmark and English document
language, exposed no unnamed interactive controls, produced no console or page
errors, stayed within the viewport, and showed a visible first-tab focus state.

## Iteration 3 - source-bound evidence and runtime

Because the shared case-study source changed, the prior visual evidence became
stale. The production-mode matrix and affected case-study captures were
regenerated. The canonical visual check now passes 42 route/viewport
observations and 24 hashed captures, including 1440, 390, and 320-pixel widths,
contrast, keyboard traversal, reduced motion, source disclosure, broken images,
duplicate IDs, and browser errors.

The resume remains a tagged, unencrypted, two-page letter-size PDF with 853
extractable words. Its title, target role, public contact line including Jamie's
phone number, and July 2026 footer are present in extracted text.

A clean Docker build on Docker 29.2.0 succeeded from Node 26. The staging-mode
container returned 200 for the homepage, resume, and CallNYC; `/api/health`
reported staging, nonproduction, and nonindexable state; `robots.txt` disallowed
all crawling; and HTML responses carried `X-Robots-Tag: noindex, nofollow` plus
the expected content-type, referrer, and permissions security headers.

## Dependency boundary

`npm audit` reports no critical or high vulnerabilities and two moderate
findings for PostCSS as pinned by the current latest Next.js 16.2.10. The npm
registry reports 16.2.10 as latest and the automated remediation proposes a
major downgrade to Next.js 9.3.3. This run does not apply that unsupported
framework downgrade or an unverified dependency override. Recheck when a
supported Next.js release updates its PostCSS dependency.

## Stop conditions

Agent-verifiable deterministic, visual, artifact, and container checks are
green. Independent exact-revision scorecards must still agree before the
composite semantic stop condition is reached.

The following remain human or external work and are not failures of the
framework: collaborator corroboration, unfamiliar-reader sessions, visual
rights and consent, bounded market response, production cutover observation,
and Jamie's approval of the exact candidate. No agent run may close them.
