# Eval Run Receipts

This directory stores redacted, exact-candidate evaluation receipts. Candidate
content hashes exclude this directory so a receipt-only commit does not change
the candidate it attests to.

A receipt records deterministic evidence only. Human, model, collaborator,
rights, deployment, and indexing outcomes remain separate and may stay pending.
