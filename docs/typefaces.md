# Typefaces

V1 uses open font names in the CSS stack without build-time font fetching.

- Karla: body text, UI, prose, navigation, metadata.
- Archivo Narrow: display headings, section headings, and proof labels.

Browsers use those fonts when available and fall back to system sans-serif fonts
otherwise. No private or proprietary font files should be committed or served.
